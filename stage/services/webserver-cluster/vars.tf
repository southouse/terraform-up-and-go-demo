variable "http_port" {
    description = "The port the server will use for HTTP requests"
    default = 80
}

variable "server_port" {
    description = "The port the server will use for HTTP requests"
    default = 8080
}

variable "ssh_port" {
    description = "The port the server will use for HTTP requests"
    default = 22
}